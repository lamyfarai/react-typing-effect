import React from 'react';
import expect from 'expect';
import { shallow, mount } from 'enzyme';
import ReactTypingEffectDemo from '../../ReactTypingEffectDemo';

describe('<ReactTypingEffectDemo />', ()=>{
  it('should pass', ()=>{
      expect(true).toBe(true);
  });
});
